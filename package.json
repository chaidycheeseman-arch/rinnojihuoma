{
    "name": "rinno",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "node scripts/sync-public.mjs",
        "prepare:public": "node scripts/sync-public.mjs",
        "dev": "node scripts/sync-public.mjs && netlify dev --port 8888",
        "license:issue": "node scripts/issue-device-license.js",
        "netlify:dev": "node scripts/sync-public.mjs && netlify dev --port 8888",
        "netlify:link": "npx netlify-cli link",
        "netlify:init": "npx netlify-cli init",
        "netlify:deploy": "npm run build && npx netlify-cli deploy --prod --dir public --functions functions"
    },
    "dependencies": {
        "@netlify/blobs": "^10.7.4"
    },
    "devDependencies": {
        "netlify-cli": "^26.0.0"
    }
}
